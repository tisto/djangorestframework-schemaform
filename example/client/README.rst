Client Example
==============

Prerequisits
------------

  $ sudo npm install -g webpack
  $ sudo npm install -g webpack-dev-server
  $ sudo npm install -g babel

Installation
------------

  $ cd client
  $ npm install
